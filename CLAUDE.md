# YouTubeアニメ紹介動画 自動化パイプライン — プロジェクトルール

チャンネル名：かさ【ゆるオタ】

このファイルはプロジェクトの永続メモリです。動画制作パイプラインに関する作業を行う際は、必ずこの内容に従ってください。

---

## 1. プロジェクト概要

**目標**：動画制作の手作業を最小化し、サムネイル作成と最終確認のみを手元に残す
**対象**：スクリプト生成・音声合成・字幕生成・動画編集の自動化パイプライン

### 完成済みパイプライン
- 台本生成：字幕用・読み上げ用の2テキストをルールに従い同時生成
- 音声生成：`voicepeak_all_in_one.py` により `merged.wav` が完成
- 字幕生成：`make_srt.py` により `subtitle.srt` を自動生成
- 配信サービス調査：康平とClaudeで役割分担済み

### 毎回の納品セット（5ファイル）
1. `台本_字幕用.txt` — 漢字表記・全要素含む
2. `台本_読み上げ用.txt` — VOICEPEAK用カタカナ・ワ変換済み
3. `配信サービス一覧.txt` — 康平がFilmarksで確認する10サービス分のメモ用テンプレート
4. `配信状況_バンダイチャンネル_アニメ放題.txt` — Claudeが調査した2サービスの○×一覧
5. `voicepeak_all_in_one.py` — 音声生成→統合の完全自動化スクリプト

### 配信サービス調査の役割分担
**康平がFilmarksで確認（10サービス）**：
U-NEXT、dアニメストア、DMM TV、Hulu、ABEMA、Lemino、Amazon Prime Video、Disney+、FOD、Netflix

**ClaudeがWeb検索で調査（2サービス）**：
- バンダイチャンネル → b-ch.com に作品ページがあれば ○見放題／△レンタルのみ／×なし
- アニメ放題 → 「作品名 アニメ放題 animehodai.jp」で検索し、animehodai.jp/title/ページがヒットすれば○、なければ×（1作品ずつ確認）

※配信状況は変動するため、動画公開前に最終確認の注記を付ける。

### ツール・リソース
- 音声合成：VOICEPEAK CLI版（ナレーター：小春六花）
- 動画編集：Filmora（Adobe Premiere Proも所有）
- 字幕編集：Vrew（SRTインポート用）またはYouTubeネイティブ字幕アップロード
- 配信情報調査：Filmarks（康平側）、b-ch.com・animehodai.jp（Claude側）
- 自動化スクリプト：`voicepeak_all_in_one.py`、`make_srt.py`
- VOICEPEAK辞書：`VOICEPEAK辞書登録リスト.txt`（表記＝読みのタブ区切り、一度登録すれば以降も有効）

### 学び・原則
- Filmoraは外部スクリプトから直接操作不可（API非公開）→ SRT生成で上流解決する方針が有効
- 漢字とカタカナの文字数差異による140字分割のズレは、読み上げ用テキストを分割基準にすることで解消
- VOICEPEAKのユーザー辞書はCLI実行時にも自動適用されるため、台本側カタカナ変換と辞書登録の二重対策が有効
- 完結済み/未完結の判定は誤りが多いため台本から除外する
- 字幕用・読み上げ用の本文内容は完全一致させる（表記の変換のみ、内容変更なし）

---

## 2. スクリプト仕様

### VOICEPEAK CLI設定
- 実行ファイルパス：`C:\Program Files\VOICEPEAK\voicepeak.exe`
- ナレーター：Koharu Rikka（小春六花）
- パラメータ：`speed=135, pitch=0, emotion="hightension=50,livid=0,lamenting=0,despising=0,narration=0"`

### voicepeak_all_in_one.py
- 最新スクリプト
- 140字制限で句点分割、空行で段落分割、改行は読点に変換
- 自動リトライ最大3回（試行間1秒待機）、各チャンク後0.7秒待機
- 失敗はfailed_chunks.txtに保存
- 統合時：ファイル間180ms間隔、末尾無音カット時に30ms余白を残す

実行コマンド：
```
python voicepeak_all_in_one.py 台本_読み上げ用.txt
```

### make_srt.py（字幕生成）
- 使い方：`python make_srt.py 台本_字幕用.txt 台本_読み上げ用.txt`（事前に音声生成が必要）
- outputフォルダの各wavの再生時間を基に、字幕用テキストの内容でタイミングぴったりのsubtitle.srtを生成
- 読み上げ用テキストを基準に句点単位で分割し、字幕用も同じ文区切りでwavと1対1対応
- 字幕用テキストから除去する要素：見出し「#」・区切り線「---」・キャッチコピー（行末が閉じ鉤括弧）・制作・著作権表記
- 句点直後の読点整形（「。、」→「。」）
- SILENCE_MSは音声統合と同じ180msに合わせる
- FilmoraはSRTインポート非対応（エクスポートのみ）のため、VrewなどSRT対応ツールまたはYouTubeネイティブ字幕アップロードで使用

---

## 3. 台本ルール

### 冒頭定型文
「今回は〇〇をご紹介します。アニメを楽しむ上で頭を使って考察しながら見るのが面白いというのは大前提としてあります。ですが今日は、そうじゃないやつ。」
→ テーマの絞り込み説明
→「それでは早速いきます。」で本編へ

### 各作品ブロック構成
1. キャッチコピー
2. 定型文「[続く/最初/最後]の作品は、〇〇。視聴できる配信サービスはこちらです。本作品のアニメ話数は全〇話の作品で、物語としては」
3. あらすじ（体言止め多用の短文リズム）
4. 魅力・見どころ
5. 注意点・視聴者への配慮
6. 著作権表記

### 構成上のルール
- 順位形式ではなく同列紹介
- 話数は「全〇話」のみ（複数期でも合計話数、「全2期」等は付けない）
- 完結済み/未完結の判定は台本に含めない
- 配信サービスは台本内では「こちらです」とのみ言及
- 20作品紹介なら必ず20件確保
- 作品情報（人名・設定・制作会社の正式名称・話数・配信サービス）はWeb検索で正確性を確認してから記載
- アニメは日本版準拠の名前を使用
  例：俺だけレベルアップな件の主人公は水篠旬 ／ 灰村諸葉の読みははいむらもろは

---

## 4. 読み上げ用テキスト変換ルール（完成版・全10項目）

1. アニメタイトルは正式な特殊読みのみカタカナ化。タイトル内の助詞「の」「と」「に」はひらがなのまま
   例：聖剣使いの禁呪詠唱 → セイケンツカイのワールドブレイク／ロクでなし魔術講師と禁忌教典 → ロクでなしマジュツコウシとアカシックレコード／落第騎士の英雄譚 → ラクダイキシのキャバルリィ
2. 誤読しやすい固有名詞・人名・地名・組織名のみカタカナ化
   例：灰村諸葉 → ハイムラモロハ／亜鐘学園 → アガネガクエン／夜ト → ヤト／笹塚 → ササヅカ／陽務楽郎 → ヒヅトメラクロウ／水篠旬 → ミズシノシュン
3. 誤読しない一般語は漢字のまま（魔法・魔王・勇者・迷宮・騎士・覚醒・前世・竜など）
4. 助詞「は」→「ワ」（ただし「では」「ならでは」は除外）
5. 括弧内読み仮名は削除
6. 固有名詞の前に読点を挿入
7. 話数の「話」→「ワ」
8. 中黒「・」→読点「、」
9. 誤読語のカタカナ化：他→ホカ／実は→ジツは／実力→ジツ力／金字塔→キンジトウ／好き→スキ／脳汁→ノウジル
10. 「話」を「はなし」と誤読するため「ワ」に変換（VOICEPEAKの既知の誤読）
